export interface IEventsExport {
  id: string;
  name: string;
  studentName: string;
  dateStart: string;
  place: string;
  rank: string;
}

export interface IEventRanks{
  id: string;
  name: string;
  rank: string;
}